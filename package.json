{
  "name": "dnssec-indicator",
  "version": "0.0.1",
  "description": "A Google Chrome extension that uses DNS-over-HTTPS (DoH) to check the DNSSEC status for each domain you visit",
  "scripts": {
    "build": "npx webpack --progress --color",
    "dev": "NODE_ENV=development npm run build",
    "prod": "NODE_ENV=production npm run build"
  },
  "author": "Trevin Avery",
  "license": "GNU GPLv3",
  "dependencies": {
    "buffer": "^6.0.3",
    "dns-packet": "^5.3.0"
  },
  "devDependencies": {
    "clean-webpack-plugin": "^4.0.0",
    "copy-webpack-plugin": "^10.0.0",
    "webpack": "^5.64.4",
    "webpack-cli": "^4.9.1",
    "webpack-images-resizer": "^0.1.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/byu-imaal/DNSSEC-Indicator"
  }
}
